# ch10 多标签主题分类器 · dev-notes

> 约定:每任务一段随做随记(用户原话/产出/偏差/纠偏);待 key 项集中列在当前任务段,不许收尾补记。

## Task 1:依赖组 ml + 权威类目表 taxonomy.py(TDD)

- **用户原话**:「能否直接进入下一阶段,先不放key」——评估:ch10 不依赖 key 的任务占大头(类目表/建表/纯函数/训练评测导出服务全链路代码与单测),需聊天上游的预标/造数验证跑照惯例进待 key 清单;训练只需联网拉基座模型,不用 API key。结论:直接推进。
- **产出**:`app/core/taxonomy.py`(17 类,类目名/边界/示例取课程 README 原表;TOPIC_CLASSES/TOPIC_NAMES/LABEL2ID/ID2LABEL/NUM_CLASSES/SEVERITY/terminology_table)+ `pyproject.toml` ml 依赖组 + `.gitignore` 加 `data/ch10/*`(reports 例外白名单)。
- **实装环境**:torch 2.14.0+cpu / transformers **5.17.0**(实为 v5大版本)/ onnxruntime 1.30.0 / tokenizers 0.23.2 / scikit-learn。Plan 写死的 v5 参数名(`eval_strategy`、`processing_class=`)在 transformers 5.17 下正确。**Windows 无 CUDA 无 MPS → 训练走 CPU**(Plan 的 `PYTORCH_ENABLE_MPS_FALLBACK` 是 mac 语境;train.py 设备自适应 cuda→mps→cpu 已兜住,CPU 训练速度入 Task 7 实测记录)。
- TDD:5 passed(17 类唯一/前四类序/字段齐全/roundtrip/术语表全覆盖)。

## Task 2:建表 + ORM + repository(TDD)

- **用户原话**:「继续」。
- **产出**:`sql/ch10-ddl.sql`(topic_classifications:question_id FK→low_confidence_questions + labels JSON)+ ORM `TopicClassification` + repository 四函数(`list_pool_texts`/`list_unclassified_questions`/`insert_topic_classifications`/`topic_distribution`)+ conftest 注册。
- **偏差①(Plan 文件缺失,同 ch09 先例)**:Plan Task 2 Step 1 称「开发库应用 DDL」引用 `sql/ch10-ddl.sql`,仓库无此文件——按 Plan ORM 定义补写并应用(开发库 SHOW CREATE TABLE 已验证)。
- **偏差②(Plan 片段与现树不符,以现树为准)**:①conftest `_TABLES` Plan 片段漏了现树已有的 `faith_cases`,按现树全清单插入 `topic_classifications` 于首位(FK 先删);②Plan 的 fixture 名 `db_session_maker` 不存在,现树是 `db_session_factory` + `db_clean`,造数走 repository 函数(`insert_review_item`/`insert_low_confidence`/`set_matched_review`),不摸裸 session;③repository 片段的裸 `async_session()` 改现树惯例 `db.async_session()`(monkeypatch 接缝)。
- 语义注:`list_unclassified_questions` 只取已归并(matched_review_id 非空)的问题——分类器只吃归并产出的标准化问法,未归并的留给下一轮归并;`list_pool_texts` 全量(训练语料含未归并原话,退回原话文本)。
- TDD:4 passed(归类后排除/未归并不进/标准化问法优先/17 类全出顺序同权威表)+ tests/db 回归 13 passed。

## Task 3:corpus_lib 纯函数(脱敏/去重/分层划分,TDD)

- **用户原话**:「继续」。
- **产出**:`scripts/ch10/corpus_lib.py`(desensitize/dedupe/split_dataset)+ 空 `__init__.py`。
- **偏差③(Plan 正则 bug,走查测试用例时抓出)**:Plan 的手机号模式 `1[3-9]\d{9}` 无数字边界,先跑会把订单号 `202601180001234567` 从中间截出 `18000123456` 误判成手机号,Plan 自带的测试用例(期望 `[单号]`)自己都过不了。修:加环视 `(?<!\d)…(?!\d)`——独立手机号照掩,长串里的 11 位不再误伤。
- **偏差④(Plan 自带测试与自带实现打架)**:`加我微信 cat_lover2026 聊` 按 Plan 实现掩成 `微信[账号] 聊`(残留尾随空格),Plan 测试期望 `微信[账号]聊`。测试即规格:模式尾部补 `[ \t]*` 吞掉 id 后的空白。
- Plan 外加一条测试:小组合(3 条)并进 label id 最小层后 val/test 不绝迹(split_dataset 的关键语义)。
- TDD:6 passed。

## Task 4:预标模块 + 黄金样例验证闸(验证跑替代 TDD)

- **用户原话**:「继续」。
- **产出**:`scripts/ch10/prelabel.py`(PRELABEL_PROMPT + prelabel_one/batch,非法标签过滤、异常/空结果兜底「其他」)+ `golden_samples.jsonl`(30 条:17 类全覆盖 + 方言/错别字/多诉求/近邻边界)+ `validate_golden.py`(集合完全一致率 ≥80% 才放行)+ Makefile `ch10-golden`。
- **环境适配(既证清单沿用)**:本机无 make,Makefile 保留作文档,直跑 `PYTHONPATH=. uv run python scripts/ch10/validate_golden.py`;validate_golden 补 `sys.stdout.reconfigure(errors="replace")` GBK 守卫(✗ 不在 GBK 里,ch09 已证必炸)。
- **无 key 行为实测**:直跑验证闸,占位上游把 openai 客户端挂到连接超时(默认 600s×重试,30 条并发 8 全在途)——无 key 时 LLM 脚本实跑无意义且极慢,**后续 LLM 验证跑一律等 key,不再无 key 空跑**;机制层用桩干跑替代:30 条解析 ✓、类目全落 17 类权威表 ✓、通过率计算 ✓、80% 压线 exit 0 ✓、错例 ✗ 打印 ✓(桩验完即弃,产物目录不收)。
- **待 key**:①`ch10-golden` 真跑(通过率 ≥80%,不过线按错例改 PRELABEL_PROMPT 近邻边界表述,不许改黄金样例凑分;连改三轮不过停下问用户);②通过后放行 Task 5 批量预标。
- 结论:模块代码就绪,闸门机制实证,真验证待 key。

## Task 5:语料流水线 build_corpus(验证跑替代 TDD)

- **用户原话**:「继续」(承接「先不放key」推进约定)。
- **产出**:`scripts/ch10/build_corpus.py`(五段编排:捞池 `list_pool_texts` → 脱敏+去重+LLM 修错别字+再去重 → 批量预标 → 按类补足到 TARGET_PER_CLASS=100(多诉求给每类都记账) → 抽审导出 `sample_review.md`(真实池全量 + 每类模拟抽 5))+ Makefile `ch10-corpus`(依赖 `ch10-golden`,闸门过线才跑)。
- **池空现状(非偏差,是上游现实)**:开发库 `low_confidence_questions` 0 条——真实池数据要等真实聊天/👎流量 + ch09 归并跑起来(全在待 key 链条上)。Plan Task 11「首遍写入约 70 条(全池)」的预期与现实不符:首次真跑时真实池可能仍是 0,语料几乎全靠模拟补足,随使用逐渐掺真。**待数据**(与待 key 并列记录):真实池有累计数据后重跑流水线。
- **干跑接缝教训(重要)**:第一版桩打在函数层(整体替换 `bc.simulate`),绕开了真实 `simulate` 的标签去重契约,桩自己造的 `["发票","发票"]` 直通产物——假阳性。改桩到 `get_chat_model` 接缝(两模块模块级导入,monkeypatch 惯例),真实 `clean_texts`/`prelabel_one`/`simulate`(SIM_BATCH=20 分轮、去重、非法标签过滤、misses<5 重试环)全跑。**顺带修了真 bug**:LLM 可能吐重复标签,`prelabel_one` 与 `simulate` 均补 `dict.fromkeys` 去重,防 counts 重复记账。
- 干跑结果(验完删桩删产物):种 4 条池数据→捞池 4→清洗修错别字(退活→退货)→预标去重+非法过滤→simulate 85 轮(17 类×5)→1685 条、16 类恰 100/发票 165(多诉求记账正确)→抽审文件结构 ✓;种数清理后池子归零 ✓。回归 325 passed。
- **待 key(顺序关卡)**:①Task 4 的 `ch10-golden` 过线;②真跑 `ch10-corpus`(上游可用+池子尽量有数);③**人工抽审检查点**:用户在对话里审 `sample_review.md`,没过这关不进真语料划分(Task 6 起代码可照写——划分/训练/评测机制不依赖真语料,合成小语料先验机制,真语料到位后重跑)。

## Task 6:build_dataset 分层划分 + 训练集增强(验证跑替代 TDD)

- **用户原话**:「继续」。
- **产出**:`scripts/ch10/build_dataset.py`(读 corpus_labeled → `split_dataset` 80/10/10 → LLM 同义句增强只扩 train → 产物 `dataset/{train,val,test}.jsonl` 每行恰 `{text,labels}` + 三份分布表)+ Makefile `ch10-dataset`。
- **Plan 外加一线防御**:增强变体之间也 `dedupe`(Plan 只防变体撞原句,不防变体互相撞);变体撞任何原句(含考题)照 Plan 丢弃。
- **桩干跑全过**(桩在 `get_chat_model` 接缝,变体加前缀):17 类×10 合成语料 → train 272(每类 8 原句+8 变体,翻倍)/ val、test 各 17(每类 1)→ 每类三份全非零、考题零泄漏(变体不进 val/test)✓;验完删桩删产物。
- **待 key**:真跑 `ch10-dataset`(依赖 Task 5 真语料 + 上游可用;三份真实分布数字到时补记在此)。

## Task 7:train.py 全参微调(验证跑替代 TDD)

- **用户原话**:「继续」。
- **产出**:`scripts/ch10/train.py`(RoBERTa-wwm-ext 全参微调:BCE 多标签、weight_decay=0.01、早停盯 val micro-F1(patience=2)、load_best_model_at_end、末尾验证集扫最优阈值 0.30~0.70 → `data/ch10/model/`(HF 权重 + tokenizer + threshold.json))+ Makefile `ch10-train`(`uv run --group ml`)。
- **v5 参数实跑验证**(此前仅查文档,这次真跑过):`eval_strategy="epoch"`、`processing_class=tokenizer` 在 transformers 5.17 下工作正常;`EarlyStoppingCallback` 3 epoch 触发早停 ✓。
- **机制干跑**(桩=BASE 换 `hf-internal-testing/tiny-random-bert` + 17 类合成 train/val):下载→编码(token_type_ids)→训练→每 epoch eval micro/macro-F1→早停→阈值扫描→save_model+tokenizer+threshold.json 全链路 ✓,threshold={0.3, 0.1111}(tiny 随机权重,数值无意义只验机制)。验完删桩删产物。
- **干跑环境坑(仅 tiny 有)**:tiny-random-bert 自带分类头尺寸≠17,transformers 5 严格加载对 mismatched sizes 直接抛 RuntimeError;干跑里包一层 `ignore_mismatched_sizes=True` 过。**真基座 chinese-roberta-wwm-ext 无分类头(全新初始化)不会触发**,train.py 不加此参数。
- CPU 训练速度观测(tiny,~2.6 万参数):72 步 4.1s。真基座(1 亿参数、128 token、batch16)的 CPU 时长等真跑记录;若不可接受,再与用户商量(Plan 写死全参微调不许自行改 LoRA)。
- **待 key/待数据**:真跑 `ch10-train`(依赖 ch10-dataset 真产物 + 联网拉 `hfl/chinese-roberta-wwm-ext`,超时按 Plan 用 `HF_ENDPOINT=https://hf-mirror.com`);到时记录训练时长/早停轮次/验证 F1/最优阈值。

## Task 8:evaluate.py 评测报告(验证跑替代 TDD)

- **用户原话**:「继续」。
- **产出**:`scripts/ch10/evaluate.py`(留出测试集:每类 P/R/F1+support、micro/macro、每类混淆矩阵、严档 F1<0.8 红线标旗;全不过线取最高分兜底不产空标签;产物 `reports/eval_report.md` + `error_samples.md`)+ Makefile `ch10-eval`。
- **机制干跑全过**(tiny 训一版供模型 → 真 evaluate 全链):17 类表行+混淆行全出 ✓、红线列 ✓、判错样本导出 ✓、极端阈值 0.99 下空标签兜底路径 ✓、`pick_device()` 本机返回 cpu ✓。验完删桩删产物(干跑报告不收,reports/ 留给真跑)。
- Plan 的 🔴/✅ emoji 只进 .md 文件(UTF-8)不进控制台打印,GBK 无碍;print 行照例加了 GBK 守卫。
- **待 key/待数据 + 人工环节**:真跑 `ch10-eval` 后**人工复核 error_samples.md**——判错若集中某类且属标注毛病,回 Task 5 改标重训(README:效果不行先搞数据);严档 F1<0.8 同样回数据。micro/macro F1 与红线达标情况到时补记。

## Task 9:export_onnx 导出 + 一致性校验(验证跑替代 TDD)

- **用户原话**:「继续」。
- **产出**:`scripts/ch10/export_onnx.py`(LogitsWrapper 包一层只回 logits → `torch.onnx.export(dynamo=False, opset 17, dynamic_axes=batch/seq)` → 测试集全量双闸:logits 容差 1e-3 + 过线标签零不一致;产物 `onnx/{model.onnx,tokenizer.json,threshold.json}`)+ Makefile `ch10-export`。
- **真 bug:机制干跑抓出「导出产物不确定」**(真基座同样中招,不修真跑必炸):同一模型连续两次导出,torch-vs-ONNX 偏差各不相同(1.3e-2 / 1.0e-2,信号量级才 3.8e-2),一致性校验时过时炸。排查路径:干净进程同文件复测 → 时好时坏 → 双导出对照 + wrapper.eval() 对照 → 定位。**根因**:torch 2.14 TorchScript 导出器按**根模块** training 标志走 HF SDPA 分支;`LogitsWrapper` 自己从未 eval(training=True 默认),train 态注意力 dropout 被随机烘进图。**修复**:`LogitsWrapper.__init__` 末尾 `self.eval()`(注释写明因果);eval 后 8.4e-9 稳定一致,全链干跑 exit 0。
- Plan 写死的 `dynamo=False` + `dynamic_axes` 在 torch 2.14 实跑验证可用(TorchScript 导出器仍在);变长 batch/seq 推理验证 ✓。
- **待 key/待数据**:真跑 `ch10-export`(依赖 ch10-train 真产物;tiny 干跑只验机制,真模型导出文件大小/校验数值到时补记)。

## Task 10:serve.py 推理服务 :8110(验证跑替代 TDD)

- **用户原话**:「继续」。
- **产出**:`scripts/ch10/serve.py`(FastAPI + onnxruntime + tokenizers 轻运行时不背 torch;`POST /classify` 批量 `{"texts":[...]}` → `{"results":[{"labels","scores"}]}`,17 类全量分数、全不过线最高分兜底;`GET /healthz`)+ Makefile `classifier-up/down`(nohup + pid 文件惯例)。
- **机制干跑两段全过**:①ASGITransport 进程内(仓库 httpx 惯例)验契约——healthz/空 texts/多变长批量/标签非空/17 类分数;②真进程真端口——`serve.py` 直启 :8110,healthz 轮询 ✓、UTF-8 文件喂 payload 的 /classify 契约 ✓、kill 后端口收回 ✓。
- **Windows 冒烟坑(环境非代码)**:Git Bash 的 curl 直传中文 JSON 会被编码搞坏(FastAPI 报 "error parsing the body"),`--data-binary @文件`(UTF-8)解决;msys 的 `/tmp` 路径 Windows Python 看不见,临时文件放仓库内。
- Plan Step 3 的语义验收(尺码+退换货等多标签句)是真模型活,tiny 随机权重只验机制 → **待 key/待数据**:真模型起服后跑三条验收句,输出补记在此。

## Task 11:classify_pool 旁路批处理(验证跑替代 TDD)

- **用户原话**:「继续」。
- **产出**:`scripts/ch10/classify_pool.py`(`list_unclassified_questions` 捞未归类 → :8110 `/classify` 整批 → `insert_topic_classifications` 写表;`--min-batch` 攒批闸 + `--force`;LEFT JOIN 幂等,已归类不重归)+ Makefile `classify-pool`。
- **端到端机制干跑全过**(tiny 模型起真 :8110 服务 + 种 12 条池数据):`--min-batch 50` 不 force 拒跑 ✓ → `--force` 真走 HTTP 归类写表 12 条 ✓ → 二遍 `--force` 报「池里没有待归类问题」且行数不变 ✓ → labels 全落权威表 ✓ → 清理后归零 ✓。
- **干跑Harness坑(非代码)**:Windows 子进程 stdout 默认 GBK,测试侧按 utf-8 解会得到乱码;子进程加 `PYTHONIOENCODING=utf-8` 解决(classify_pool 本体只打中文,GBK 控制台无碍,不加守卫)。
- **待 key/待数据 + 现实校准**:Plan「首遍写入约 70 条(全池)」——当前开发库池子 0 条,首次真跑只会报「池里没有待归类问题」。真实池归类分布输出待真实使用积累数据后补记;cron 接入属部署期事项。

## Task 12:主题分布 + 类目问题列表 API(TDD)

- **用户原话**:「继续」。
- **产出**:`app/api/topics.py`(GET `/api/topics/distribution`;GET `/api/topics/questions?label=&page=&size=`,label 先对权威表校验,未知 400)+ `repository.topic_questions`(命中判断/分页 Python 侧)+ main.py 注册 + `topic_distribution` 样例按展示文本去重(计数不受影响)。
- **偏差(沿用 Task ②惯例)**:Plan 测试用 `db_session_maker` + 裸 session 造数——现树无此 fixture,改 conftest `client` 惯例 + repository 造数。
- **TDD**:RED 8 failed → GREEN 8 passed;全量回归 333 passed。
- **测试基建发现**:conftest 全局 `client` fixture 不挂 `db_clean`,跨 pytest 运行的残留行会让计数断言翻车(实测残留 31 行);凡自己种数的 API 测试照 review 惯例本地起 client(`db_session_factory, db_clean`)。
- **实现小坑**:`ReviewQueue` 审核列名是 `review_status` 不是 `status`(Plan 伪代码没写这层,实现时按模型实名列取)。

## Task 13:主题分布页 + 类目问题列表页(Vibe Coding 第一版)

- **用户原话**:「继续」(承接「先不放key」推进约定)。
- **产出**:`app/static/topics.html`(顶部统计条 + 17 类纯 CSS 横向条形图,按 count 降序、0 计数淡显、details/summary 展开样例、类目名与「查看全部 N 条」均链到 `/topics/questions?label=`)+ `app/static/topic-questions.html`(类目 chips 按量排开当前高亮、标准化问法在上原话在下、labels/来源/同义合并数/审核状态/归类时间一排、上一页下一页;**类目与页码全走地址栏**,无 label 参数时 `location.replace` 落到问题最多的一类)+ admin.js 注册表加「主题分布」模块行 + main.py 两条静态路由(照惯例赶在 catch-all mount 之前)。
- **Plan 外扩了 API 字段**:页面规格要展示来源/同义合并条数/归类时间,但 `topic_questions` 原本只回 question_id/labels/text/raw/normalized/review_status——repository select 补 `LowConfidenceQuestion.source`、`ReviewQueue.occurrence_count`、`TopicClassification.classified_at`(ISO 串),item 三字段齐上;测试加一条字段往返断言,9 passed,全量回归 334 passed。
- **Vibe 循环现状**:本会话无浏览器/截图工具,Plan Step 2 的「起服务截图给用户」改为交付 URL 由用户自己开页反馈——`http://localhost:8000/topics` 与 `/topics/questions`(需先起主应用;条形图/空态样式等用户看了再改,改了几轮、都改了什么待本段追记)。开发库池子现为 0 条,页面首见是全零淡显 + 空态文案,属预期。
- **待数据**(非 key):池子有真实归类数据后页面即有内容;样式迭代意见随时改。

## Task 14:结构化验收产物(各脚本 .md + .json 一式两份)

- **用户原话**:「继续」(承接「先不放key」推进约定)。
- **产出**:五份 json 落 `data/ch10/reports/`(golden/eval/threshold_scan/export/classify_run)——evaluate 的指标先算齐再两份渲染(md 人读、json 页读,同一份数据);判错样本按 missed/extra 组合记账(kind=漏打/多打/错位,total_cells=错例矩阵笔数);新建 `scan_threshold_replay.py`(验证集经 :8110 打分一次、分数表固定、九候选线各算 micro-F1,consistent=重演当选线==在用线;口径与 train.py 一比一:裸 `scores>=t` 无兜底、严格大于留任先到);Makefile 加 `ch10-threshold-scan`、`classify-pool` 支持 `FORCE=1`。
- **偏差⑤(Plan 未钉中档红线值)**:Plan 通篇只写「严档 0.8」,但 eval_report.json 的 passed 三值要求严/中给 bool——中档线取 **0.6**(低于严档、仍是有约束的线),RED_LINES={严:0.8, 中:0.6, 宽:None},md 表同步按三档渲(宽档画 —)。用户嫌松/紧随时改这一个字典。
- **偏差⑥(export 失败要写报告,原 assert 挡路)**:一致性校验原来是 `assert np.allclose(...)` 一票炸——炸了报告就落不下来,「失败也要写」落空。改成按行计账:超容差行与过线翻面行都计入 mismatch,先写 export_report.json 再 SystemExit。
- **干跑 25 项断言全过**(tiny 占位模型 + 合成考卷 + 桩 golden,验完全删):eval 三值 passed/矩阵账=错例账/md 条数=json;export checked/mismatch0/opset/bytes;replay 两路都验(在用线故意写错 → exit1+consistent False;写对 → exit0+True)且「打平先到留任」单独验;golden 两路(过线 exit0、全错不过线 exit1 但报告照落)且重复标签/非法标签过滤后仍算对。
- **真跑**:classify-pool 池空 → `classify_run.json {status: empty}` 落盘(真实产物,已入 git);二遍 empty 即幂等实证。其余四份 json 的真产物全在待 key 链上(golden 真跑/训练→评测→导出→重演)。
- **自伤小坑**:干跑断言拿全精度 29/30 与报告里 round(…,4) 的 rate 比,1e-6 容差必炸——产物口径(落盘即舍入)先看清楚再写断言。
- **待 key**:golden/eval/export/threshold-scan 四份真报告落地时,`threshold_scan.json.consistent` 应为 true(训练扫描与线上重演同口径)。

## Task 15:验收 API + 白名单作业运行器(TDD)

- **用户原话**:「继续」(承接「先不放key」推进约定)。
- **重大坑(自伤后恢复)**:Plan 写「Create `app/core/jobs.py` 等」,但同名文件 **ch03/ch07/ch09 就有**(rageval 测试还依赖注册表里的 `eval-rag`)——直接新建把三个老文件覆盖掉了,全量回归 7 个 rageval 用例 KeyError('eval-rag') 才暴露。恢复:老 API 面逐一对齐(`JobSpec(name,target,heavy)`、`_procs` 活进程表这个 monkeypatch 接缝、`JobNotFound`/`JobAlreadyRunning`、`status()` 返回 dict、`Popen(["make", target])`),在其上做 ch10 扩展而不是替换;`app/api/jobs.py` 从 HEAD 原样 checkout 回来(老路由本就够 Plan 用);两个测试文件改成「老用例全保留 + ch10 追加」的合并版。教训入记忆:**Plan 说 Create 先查 `git log` 该路径是否已有历史**。
- **合并后的运行器设计**:`JobSpec` 加 `needs`(页面按钮直接提示前置)与 `force`(同一条 make 配方加 `FORCE=1`,不复制命令);`_procs` 保持老接缝,另起 `_meta` 簿记表(stopped/returncode/起止时间);状态机五态 idle/running/pass/fail/stopped,**stopped 最先判**——人为 kill 的非零退出码不冤枉成 failed;`work()` 线程 try/finally 保终态,炸了记 rc=-1 不卡 running。
- **无 make 环境的直译(平台适配)**:有 make 走 make 原口径;没有则 `start()` 代读 Makefile 把配方直译成本地命令——支持本仓库全部在用构造:依赖递归展开(ch10-corpus 先跑 ch10-golden)、`$(if $(FORCE),--force,)` 看 spec.force、`$(or $(X,d))` 读环境变量带缺省、`PYTHONPATH=.` 前缀转 env、`< 文件` 转 stdin;遇到不认识的 make 语法明着抛 `RecipeNotTranslatable` 不硬猜。直译失败在记账**之前**抛,不留假 running。
- **classifier-up/down 原生托管**:这两个配方本体是壳脚本(nohup/sleep/curl/kill),直译无意义——运行器原生托管:up 同步拉起 `uv run --group ml python scripts/ch10/serve.py`、pid 落 `data/classifier.pid`、轮 healthz 最多 ~15s;down 按 pid 杀树(Windows taskkill /T /F,POSIX killpg)。
- **竞态三连修**(红→绿过程中暴露):① start() 与工作线程登记 `_procs` 之间有窗口,`status()` 补「meta 已记账且无退出码 ⇒ running」的判断,否则刚发起即查是 idle;② stop() 在进程登记前调用会扑空,改成无进程也先落 stopped 标记(线程循环顶看到标记即退出);③ 没跑过的作业 stop() 依旧无害(既无 proc 也无 meta 才直接返回)。测试配套 `_await_proc`/`_drain` 轮询助手。
- **验收 API 设计决定**:`/api/acceptance/overview` 九闸(黄金预标/语料/考卷泄漏/训练三件套/测试红线/ONNX 导出/推理服务/阈值可复算/旁路落库),三态 **pass/fail/missing——「没跑过」≠「没过线」**,产物缺失时 note 不写失败态文案(测试拿失败词表断言);闸门只读落盘 json 与文件 stat,**零重算**(泄漏检查是对三份 jsonl 的文本集合求交,不是重新划分);服务在线探测超时 1.5s,离线归 missing 不归 fail;pool 闸只有 pass/missing 两态(脚本自身三态 empty/below_batch/done 都算链路通);主题分布一段 mysql 读不到单独降级不连坐;`/errors` 的 RECIPES 是判错方向→处置建议的静态对照,不是按数据算的。JobSpec 没有 title 字段,overview 的 jobs 字典以 make 目标名充当标题。
- **测试**:core 层 8 用例(注册表 21 条且目标都在 Makefile、idle 初态、重入抛、stopped 不改写 failed、按退出码 pass/fail、FORCE 变体同配方、直译的 FORCE/env/依赖顺序);API 层老 3 + 新 3(stop→stopped、列表覆盖全注册表、force 变体);acceptance 层 9 用例(tmp_path 换产物目录,九闸缺产物全 missing 且文案无失败词、jobs ⊆ 注册表)。
- **回归**:351 passed(含 rageval 老用例)。

