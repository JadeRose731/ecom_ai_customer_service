# ch09 dev-notes(可观测性与数据飞轮)

## Task 1:ORM + conftest + repository 地基

- **用户原话**:「继续」(按章推进约定;Plan 中 `sql/ch09-ddl.sql` 标注为用户手写已提交)。
- **关键产出**:`sql/ch09-ddl.sql` + `ReviewQueue`/`EvalRun` ORM + `LowConfidenceQuestion` 两列 + repository 10 方法 + conftest 挂 DDL;dev 库已应用并验证(两新表 + matched_review_id 列带 FK 索引)。
- **偏差记录(Plan 说 DDL「已提交」实际缺失)**:磁盘与 git 全历史均无 `sql/ch09-ddl.sql`。按预授权「走不通按推荐的来」:表结构在 Plan Task 1 Step 4 的 ORM 里已完整定稿(列名/类型/默认值/ENUM 值全有),故按仓库 DDL 风格(ch08 单表注释风格 + ch07 ALTER 追加风格)反推写出,含三个务实索引(review_status/updated_at/matched_review_id)。**影响**:若用户手上有自己的定稿版,直接覆盖本文件重跑即可,ORM 侧无需改动(逐列一致)。建议用户过目一遍该文件。
- **纠偏(Plan 测试代码漏 `db_clean`)**:Plan 的 `test_ch09_flywheel_tables.py` 只挂 `db_session_factory`,行数据跨用例泄漏(ids 1/2/4/5 混进断言,3 条假失败)。按仓库惯例(test_low_confidence.py 等)补挂 `db_clean`。
- **纠偏(Plan `_TABLES` 丢了 `faith_cases`)**:Plan 给的删除清单把 ch04 在用的 `faith_cases` 删了;清单意图是「加两新表」不是「删旧表」,保留之。
- 全量 259 passed(252 + 7 新)。

## Task 2:Langfuse 部署物 + 配置 + 编译时挂回调

- **用户原话**:「继续」。
- **API 口径以安装版实测为准**:`uv add langfuse` 实装 **4.15.4**(v4 SDK),逐条查证:
  1. `Langfuse(public_key=, secret_key=, host=)` ✓(host/base_url 两参名都收,取 README 口径 host);
  2. `langfuse.langchain.CallbackHandler()` 无参构造 ✓;handler 源码确认 config metadata 映射:`langfuse_session_id`→session_id 等(runtime 三处 config 的 metadata 写法成立,实测 trace 落对 session);
  3. **偏差①(重写)**:`get_client().update_current_trace()` 在 4.x 已删除;且服务端 v4 是 **events_only 模式**——REST `POST /api/public/traces` upsert 404、`api.trace.get/list` 退役,「事后补写 trace」路线整条不通。**替代**:tag_intent 走 trace 级 OTel 属性(见偏差④)。
- **偏差②**:Plan 说「Langfuse v3 自部署」,官方 compose main 现发 **v4 镜像**;SDK 已是 4.15.4,服务端与 SDK 同代对齐,维持自部署选型不变,取 v4。
- **偏差③(v4 Metrics API 实测口径,Task 11 依赖)**:`api.metrics.get` 不存在;可用的是 `api.metrics.metrics(query=json.dumps({...}))`,且 **traces 视图不支持(events_only),只有 observations 视图**;字段名 `metrics`(非 measurements)、聚合键 `aggregation`(非 agg);tags 过滤必须 `type: "arrayOptions"` + `operator: "any of"` + 数组值。cost 脚本按此口径写。
- **偏差④(意图标注机制,考证链全文)**:Plan 设计「tag_intent 里 update_current_trace 补 tags/metadata」在 v4 双重不可用后,对 SDK/服务端源码+实机做了系统排查:
  1. **tags 是 trace 级 OTel 属性**(`langfuse.trace.tags`),observation 没有自己的 tags 列;Metrics observations 视图的 tags 维度按「**各 span 自身导出时携带的该属性**」分组;
  2. **LangGraph 每个节点跑在独立 asyncio task**(上下文副本),propagate_attributes 的 attach 出不了本节点 task——classify 节点里 attach,后续节点建的 LLM span 拿不到(对照组实证);
  3. span_processor `on_start` 会把 propagated 属性复制到新建 span 上 → **产生模型调用的节点入口各自 attach 一次**,该节点内后续 LLM observation 全带 tag(实机验证:`intent:商品咨询` 正确分组);
  4. **坑(已绕开)**:`propagate_attributes(...).__enter__()` 丢弃 CM 临时对象会被 GC,generator 关闭触发 `finally: detach`,attach **立刻被撤销**(`with` 块写法因持有引用而正常)——此前「metadata kwarg 之谜」实为该 GC 效应的误判。绕开姿势:直接 `otel_context_api.attach(set_value("langfuse.propagated.tags", tags, current))`,不用 CM、不持 token;LangGraph 节点 task 结束上下文副本自然丢弃,无泄漏。键名 `langfuse.propagated.tags` 为 SDK 4.15.4 内部常量(升级 SDK 需回归 smoke)。
  5. trace 详情页显示 tags 另走通道 1:handler 挂的 root observation 图终才导出,运行中 `root_span.set_attribute("langfuse.trace.tags", tags)` 即时生效。
  - 结论写进 `tag_intent` docstring;**节点接线(Task 3/5)**:classify_intent 调一次(trace 级 + 本节点),产生模型调用的节点(agent_llm/forced_rag/retrieve_policy)各自再调一次(按意图分组建模靠这个),比 Plan 的「只挂 classify 一处」多两处,偏差已记。
- **部署适配**:唯一宿主端口撞点是 langfuse minio 控制台 9091 ↔ milvus → 挪 19091;redis 宿主 6379 被 Windows 本机 redis-server 占 → 挪 16379。headless 初始化变量官方 compose 已内置(`LANGFUSE_INIT_*`),填 MewHelp 固定 key;TELEMETRY_ENABLED 关。**坑**:仓库 `.env` 的 `DATABASE_URL`(应用库 mysql)会经 compose 插值毒化 Langfuse Prisma(P1013 crash loop),已在 compose 里写死本栈 postgres URL。
- **实机验证已过**:栈 healthy、/api/public/health 200、headless key 认证 OK、合成 trace 摄入、metrics 查询正常、smoke-14 跨节点意图打标(上表 3)。真实业务轮次的 trace 树/成本单价比对留待真实模型 key(记本章待 key 清单)。
- **提交纠偏**:Task 1 提交(be11c92)漏了 dev-notes/ch09.md 本身,随本任务一并入库。
- 全量 267 passed(259 + 8 新:观测模块 8 条,含双通道/并发串扰/降级路径)。

## Task 3:token usage 收口 + 意图打进 trace

- **用户原话**:「继续」。
- **Step 1 查证(安装版源码,Context7 本环境不可用)**:langchain-openai **1.6.1** 的 `ChatOpenAI.stream_usage: bool | None = None` 存在(chat_models/base.py L824),v1.x 里即旧 `stream_options={"include_usage": True}` 的收口参数,Plan 前提成立。
- **偏差(Plan 的「main_agent 去重复 bind」)**:现树 grep 全无 `stream_options`/`include_usage`——Plan 针对的重复 bind 不存在(代码从未有过该行),收口只剩 llm.py 加 `stream_usage=True` 一行,无删除动作。
- **tag_intent 接线比 Plan 多两处**:Plan 只挂 classify_intent;但 Task 2 已实证 LangGraph 节点 task 边界下 attach 不跨节点,按意图分组要求「产生模型调用的节点各自 attach」。本任务接 classify_intent(带 session_id=conversation_id 防并发串扰)+ **agent_llm**(最大成本口,Plan 本就要动该函数);forced_rag/retrieve_policy 留 Task 5 一并接。
- **待真实模型 key 验证(冒烟)**:真实轮次 trace 的 generation span usage 数字、Langfuse UI trace 详情 tags 展示——机制层已由 smoke-11/14(合成 span)验证;真模型端到端入本章待 key 清单。
- 全量 269 passed(267 + 2 新)。

## Task 5:置信度闸接线(state + forced_rag + fallback_reply)

- **用户原话**:「继续」。
- **偏差(节点名)**:Plan 写 `retrieve_knowledge`,现树节点自 ch05 起叫 `forced_rag`,测试与实现均按现名(Plan 引用名过时,行为等价)。
- **偏差(search_knowledge 签名)**:Plan 新版传 `bm25_text=` 关键字——现树 `search_knowledge(query, strategy, top_k, category, ...)` 无该参,沿用 ch05 的「扩展词拼进检索串」姿势,未改检索器。
- **tag_intent 接线(承 Task 2 考证,Plan 外)**:forced_rag(含 understand/selfcheck 两次 LLM)与 retrieve_policy(含 expand_queries LLM)入口各加一次,退款子流程的模型成本也能按意图分组;classify/agent_llm 已在 Task 3 接。
- **旧机械闸字段退役**:`trace.evidence_top` 全线换 `evidence_confidence`+`confidence_signals`(Plan 预告的口径升级);闸的判据从 rerank_min_score 换四信号总分 vs 校准阈值(占位 0.5)。
- **旧测试纠偏**:test_reply_nodes 的 fake_insert 补 `retrieved_chunks=None` 形参(接口升级,Plan 测试代码同样漏改这里)。
- 全量 282 passed(277 + 5 新)。

## Task 6:阈值校准脚本(实跑待 key)

- **用户原话**:「继续」。
- **产出**:`scripts/calibrate_confidence.py`(ABC 可答桶 vs D 应拒桶各算 evidence_confidence 分布,0.05-0.95 扫阈值取 Youden J 最大分离点,报告落 dev-notes/ch09-confidence-calibration.txt)+ Makefile `calibrate-confidence` 目标。
- **两处实用修正(Plan 外)**:①评估集 `open(..., encoding="utf-8")`(Windows GBK 默认编码会炸,且路径以 `_ROOT` 锚定不依赖 cwd);②干跑验证:打桩 search_knowledge 全链路跑通(分布表/扫描/推荐值/报告落盘),确认脚本本体无 bug 后删除合成报告——**产物目录里的校准报告必须是实跑出的**。
- **待 key(本任务核心阻塞)**:实跑校准需 硅基流动 embed(401 已实测)+ rerank key;`make calibrate-confidence` 在 key 就位后执行,推荐阈值回填 `settings.evidence_confidence_threshold`(现占位 0.5,注释已标)。**若实跑两桶重叠严重(J < 0.3),按 Plan 约定停下来给用户看分布,不许硬选。**
- 全量 282 passed(本任务无新测试,数据校准类,产物即验证)。

## Task 7:用户反馈入口(👎 API + 快照回捞 + 前端接线)

- **用户原话**:「继续」。
- **产出**:`POST /api/feedback`(schema Literal up/down)+ `runtime.get_turn_snapshot`(checkpointer 终态回捞最近一轮问题与召回快照)+ 前端反馈条接线。
- **实现按 Plan 全量落地**,两处前端适配(Plan 预授权「细节按现有 DOM 命名适配」):①用户问题回捞走 `closest('.row.bot')` 后向前找 `.user` 行取 `.bubble` 文本(现有 DOM 是 row>bubble 两层);②会话 id 用现有全局 `conversationId`(字符串),请求时 `Number()` 转换(与聊天发送同款);失败静默 `.catch(()=>{})`。
- **待真实 key/环境验证(浏览器验收)**:发一问 → 👎 → 查 `low_confidence_questions` 出现 user_feedback 行且走过检索的轮次 retrieved_chunks 非空——机制层 5 条 API 测试已覆盖(含回捞失败降级、问题不匹配不硬塞快照);端到端待真实模型 key 起完整服务后人工过一遍。
- 全量 287 passed(282 + 5 新)。

## Task 8:飞轮流水线(标准化+查重 + 批处理 + 样例验证)

- **用户原话**:「继续」。
- **产出**:`flywheel.normalize_and_match`(structured output 一次出三件:标准化问法/命中候选 id/示例答案)+ `process_pending`(游标=matched_review_id IS NULL,串行逐条,同批同义归并到刚建的行;命中任何状态只累加不复活;解析失败/幻觉 id 跳过留游标)+ `make flywheel` / `make flywheel-samples` + 12 条标注样例。
- **纠偏(Plan 测试代码又漏 `db_clean`)**:test_flywheel.py 五条全落库,不清理会跨用例串行——按仓库惯例补 autouse `_clean_db`(同 Task 1 教训)。
- **脚本鲁棒性**:两个 scripts 都以仓库根锚定路径 + 显式 sys.path(不依赖 cwd/PYTHONPATH 也能单跑;make 目标照旧给 PYTHONPATH)。
- **待真实模型 key**:①`make flywheel` 实跑(批处理端到端);②`make flywheel-samples` 标注样例通过率 ≥ 80% 验证——纯 Prompt 任务的「TDD 替代」,key 就位后先跑样例,不到线改 prompt 重跑(连续两轮不到线停下来问用户)。机制层 5 条单测已覆盖查重语义(归并/不复活/幻觉 id/失败跳过/同批串行)。
- 全量 292 passed(287 + 5 新)。

## Task 9:审核 API + 通过写回知识库

- **用户原话**:「继续」。
- **产出**:队列/详情/通过/驳回四口(`app/api/review.py`);approve = 核准答案以 QA chunk 走 ch03 落库流程(write_pending → vectorize_pending 同步),**向量化成功才置「通过」**,失败 502 状态保持待审可重试;写回行 category=`飞轮沉淀`;驳回/通过皆终审(再操作 409)。`/review` 页面路由先挂,占位页防 404(本体 Task 12)。
- **偏差⑤(Plan 引用的「既有约定」不存在)**:Plan 说 vectorize_pending 违反「Milvus 同步调用固定单线程执行」约定且已有 `acall`——现树 retrieval.py 本就是 async 里直接同步调 Milvus,全库无 acall、无线程机制(grep 实证)。按预授权走推荐:①`milvus_client.acall` 新建,即 `asyncio.to_thread` 壳——线程池而非 Plan 字面的「固定单线程」(现树无专用线程可复用;pymilvus gRPC channel 线程安全;要害是批量 upsert+flush 秒级不能占住 server 的 loop);②`vectorize_pending` 的 client 改可选(缺省惰性取单例),upsert 闭包内带 ensure_collection(幂等,server 场景自足);显式传 client 的调用方(kb.py 两处 / vectorize_kb.py / 两个既有测试)行为不变,Milvus 8 条测试零回归。
- **纠偏(测试形状)**:Plan 的 TestClient + `run_until_complete` 造数形状按其自己备注换掉——httpx ASGITransport 同 loop + async 测试(与 conftest client / Task 7 feedback 测试同款);Plan 又漏 `db_clean`,挂 client 夹具上兜跨用例残留(Task 1/8 同一教训)。
- 全量 297 passed(292 + 5 新)。

## Task 10:自动化评估流水线(eval_runs + 趋势对比)

- **用户原话**:「继续」。
- **产出**:`scripts/eval_flywheel.py` + `make eval-flywheel [TRIGGER=手动|定时]`——hybrid_rerank Recall@10/MRR + Faithfulness + D 桶拒答率(线上 query_faq 端到端含两道闸),每轮落 eval_runs,读最近 10 轮连趋势,下滑标 ⚠,报告落 dev-notes/ch09-eval-trend.txt。
- **偏差⑥(Plan import 的 5 个符号 4 个不存在)**:`_format_evidence`→现树 `_evidence_text`;`_hit_rank(h, s["expect_section"])`→现树 `_group_rank(hits, aliases, limit)`(且 Plan 写法对 E_multi 会 KeyError——该桶没有 expect_section,只有 expect_sections_all 多组结构);`_retrieve`→`retrieval.search_knowledge(strategy="hybrid_rerank", top_k=K)`;`_refusal_one` 在 eval_ch04 是 `_generation` 的内嵌函数不可 import→本地重写、走同一个 `query_faq` 线上管线(sufficient is False 判拒);`_mean` 现树无→本地三行补。按 Plan 自己「import 复用不复制粘贴」的原则:`_Faith` 裁判模型与并发/超时常量(RETR_CONCURRENCY/GEN_CONCURRENCY/CALL_TIMEOUT)直接 import 复用。语义对齐:E 桶沿用 ch04 多组口径(组命中占比/组名次倒数均值);A–D 的 Recall 窗口取 K=10 与指标名 recall_at_10 一致(ch04 生产评估是 RECALL_K=5——两把尺子各有其用,趋势线看的是轮间一致性)。
- **Windows GBK 守卫**:main() 开头 `sys.stdout.reconfigure(errors="replace")`——干跑实证 `⚠` 在 GBK 控制台直接 UnicodeEncodeError;趋势报告文件始终 encoding="utf-8" 不受影响,控制台降级成 ? 不炸定时任务。
- **干跑验证(延续 Task 6 纪律:产物目录只收实跑产物)**:打桩检索/生成/裁判/拒答/落库,300 题全量聚合跑通(覆盖 E 多组、0-rank、裁判 None 剔除、拒答半数四条路径),趋势表 ↑/⚠↓/→ 逐格核对 + 末行「⚠ 下滑指标」汇总正确,随后删干跑架与合成报告。
- **待 key**:`make eval-flywheel` 实跑两轮(手动+定时各一)落 eval_runs 两行、趋势肉眼验收——embed 上游 401 已实测,入本章待 key 清单。
- 全量 297 passed(本任务无新测试,评估工具类)。

## Task 11:Cost Control 统计脚本(按意图 token 账)

- **用户原话**:「继续」。
- **产出**:`scripts/cost_by_intent.py` + `make cost-report [DAYS=7]`——意图 × 请求数 × 总/均 token × 占比,降序 + 最烧钱标记,报告落 dev-notes/ch09-cost-report.txt。
- **偏差⑦(v4 API 口径,承偏差③,本次对活服务 v4.38.0 逐条实测钉死)**:Plan 脚本三处全不可用——①`client.api.metrics.get` 不存在,SDK 4.15.4 方法是 `api.metrics.metrics(query=json.dumps({...}))`,底层 **GET** `api/public/v2/metrics`(query 作 URL 参数,不是 POST body);②traces 视图 events_only 不支持(Task 2 已证),用 observations 视图 + `type=GENERATION` 过滤(LLM 调用数);③行结构是 dict:`.data` → `[{"tags": [...], "sum_totalTokens": N, "count_count": N}]`(维度字段 + `{measure}_{aggregation}` 键,tags 是列表非标量)。`totalTokens` measure 名在 v4 观测视图合法(SDK raw_client docstring 内嵌全 schema:inputTokens/outputTokens/totalTokens/totalCost 等)。Plan 的「主路分组+回退逐意图」结构保留:主路 tags 维度一次分组;回退逐意图 `arrayOptions`+"any of"(Task 2 smoke 已验证的形状)。另:意图清单不硬编码 Plan 的九类(含已不存在的「人工」),直接 import `app.core.intent.INTENTS` 八类,单一来源。
- **语义注**:按 tag 分账只覆盖图内模型调用(节点入口 tag_intent 打标);图外调用(飞轮标准化/评估脚本)无 intent tag,不进意图账——这是设计使然,README 口径「按意图把 token 分堆」。自定义模型无内置单价,统计以 token 为准(Plan 同款说明)。
- **实跑验证(真服务真数据)**:`make cost-report` 对活栈跑通主路,捞出 Task 2 冒烟期两条带 tag 的 generation(商品咨询/同节点llm 各 1 次调用,假模型 token=0)——表格/归并/降序/落盘全链路 OK。**当前报告数字是冒烟期数据**,真实模型轮次的账(含与 Langfuse 界面对数)入本章待 key 清单:先真实聊几句再重跑 `make cost-report` 覆盖报告。
- 全量 297 passed(本任务无新测试,外部 API 查询脚本,实跑验证)。
