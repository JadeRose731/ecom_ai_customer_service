# dev-notes · ch01 纯对话

> 按 spec「工作要求 #2」追记:用户关键原话 / 关键产出 / 拒绝与纠偏 / 翻车与返工。

## 2026-09-09 开工

- **用户原话**:「按照文档从ch01开始跑,ch01完成后就推送一次,ch02……以此类推,数据库的账号密码都是root,其他密钥先占位,我最后提供。」
- **产出**:`ch01/Plan/ch01Spec.md` 全文通读,按 Plan 逐任务 TDD 执行(失败测试 → 实现 → 通过 → commit)。

## 环境适配(开工前)

- 机器:Windows 11,无 make;Plan 的 `make dev` 在本机不可用,保留 Makefile 文件本身,实际以 `uv run` / Git Bash 直跑。
- uv 0.12.9 + uv 托管 Python 3.12.11,与 Plan 要求一致。
- 仓库根目录已有设计文档(ch01–ch10),代码直接落在根目录,与 Plan 内的 `app/`、`tests/`、`scripts/` 路径一致。
- Plan Task 8 要求写 README:仓库已有 README(设计文档库说明),选择**合并**而非覆盖,在原 README 增补「快速开始 + ch01 验收命令」。

## Task 1–7(脚手架 → schema → memory → prompts → llm 工厂 → /api/chat → /api/extract)

- **产出**:每任务一次 commit(c50e6c7…c90cf57),全部先红后绿:
  - Task 1 `tests/test_config.py` 2 passed —— 三项账号配置无默认值,缺了启动即报
  - Task 2 `tests/test_schemas.py` 4 passed
  - Task 3 `tests/test_memory.py` 4 passed(trim_messages + count_tokens_approximately,strategy="last"、start_on="human")
  - Task 4 `tests/test_prompts.py` 2 passed(锁模板结构,效果验证留 Task 8)
  - Task 5 `tests/test_llm.py` 2 passed(断言跟随 settings,不写死模型名)
  - Task 6 `tests/test_chat_api.py` 3 passed —— 按 Plan 提醒先用 `python -c` 实测 `FakeListChatModel.astream` 逐字符出块,`len(deltas) > 1` 断言成立,未改断言
  - Task 7 `tests/test_extract_api.py` 3 passed(with_structured_output;上游异常 → 502)
  - 全量回归:`uv run pytest` **20 passed**
- **翻车与返工(环境类,均就地解决,未改方案)**:
  1. Windows 控制台 GBK:curl 直接传中文 body 报 `error parsing the body`(GBK 字节不是合法 UTF-8 JSON)→ 改 Write 工具写 UTF-8 文件 + `curl --data-binary @file`。这正是 Plan 在 dev.sh 里预埋 `PYTHONUTF8=1` 的同源问题。
  2. Git Bash 的 `/tmp` 对 Windows Python 不可见(FileNotFoundError)→ 临时文件放项目目录,用完即删。
  3. 测试输出中文测试名乱码 → 命令统一带 `PYTHONUTF8=1`。

## 上游连通性(待真实 key)

- Task 5 Step 6 冒烟:`.env` 中 key 为占位符,`$CHAT_BASE_URL/chat/completions` 无响应 —— 按 Plan「连不通就报告用户」处理,**不做任何方案替换**。
- 占位 key 下错误路径已验证:起服务后 `/api/chat` 返回 SSE `event: error` 帧、`/api/extract` 返回 502,均为设计内行为,流不断、不崩。
- **待办(用户提供 key 后)**:重跑 Task 5 冒烟 → `scripts/eval_extract.py` 5/5 → `scripts/demo_chat.sh` 两轮上下文 → 补记本文件。

## 完结交付

- 功能演示:README「快速开始(当前进度:ch01)」一节。
- 测试结果:20 passed(单测,不打真实模型);真模型验收三项待 key。
- 本章 commit:c50e6c7 → c90cf57(Task 1–7)+ 本收尾 commit。
