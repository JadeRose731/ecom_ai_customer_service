# ch08 开发笔记(即插即用工具系统)

## Plan 审查结论(开工前)

四处 Plan 偏差/适配,按用户预授权规则(走不通按推荐项来并作记录)处理:

1. **`sql/ch08-ddl.sql` 缺失**:conftest `_DDL_FILES` 与 Task 1 Step 6(dev 库应用)都引用它,Files 清单没列 Create(ch07 同款缺口)。已在 Task 1 补建:`tool_audit_logs` 13 列,对齐模型定义,不挂外键(审计不能被引用约束拦),utf8mb4 惯例同 ch02/ch06。
2. **测试实名漂移**:`tests/test_tools_infra.py` 里期望集合测试实名 `test_registry_has_six_tools`(Plan 写 `test_registry_has_all_tools`),Task 2 按实名改。
3. **连带扫描**(Plan Task 2 Step 5 要求 grep 全库,补充 Plan 未点名的三处):`scripts/bare_agent_loop.py` 依赖 `infra.execute_tool_call` + `registry.get_all_tools`(Task 5 删 infra 时需同步处理);`app/core/prompts.py:40` AGENT_SYSTEM 固定提及 `query_logistics`(内置下线后文案过时,Task 5/6 改 prompt 时一并更新);`scripts/eval_agent.py` 期望内置 `query_logistics` 自调(ch02 旧脚本,加注记不改逻辑)。
4. **SDK API 实装裁定**(Task 1 Step 1 探测,mcp 1.30.0 / langchain-mcp-adapters 0.3.2 / jsonschema 4.26.0):
   - `from mcp.server import MCPServer` **不存在** → 走 Plan 兜底分支 `mcp.server.fastmcp.FastMCP`;host/port 在**构造器**参数里(`FastMCP(name, host=…, port=…)`),`run(transport="streamable-http")` 不收 host/port。
   - `MultiServerMCPClient(handle_tool_errors=…)` 存在 ✓;`get_tools(server_name=…)` 存在 ✓。
   - dev-notes 后续按 FastMCP 路径记录。

环境沿用台账:[[mewhelp-env-adaptations]](PYTHONUTF8=1、MySQL 3307、无 make → `uv run pytest`);Makefile 的 mcp-up/mcp-down 目标按 POSIX 交付(nohup/kill),本机 Windows 用 PowerShell 手动起停验证;测试库/开发库均 docker mewhelp-mysql。

## Task 1:审计地基(依赖 + settings + ToolAuditLog + repository + 测试库接线)

- 依赖:`uv add mcp langchain-mcp-adapters jsonschema` → mcp 1.30.0 / adapters 0.3.2 / jsonschema 4.26.0;API 探测结论见「Plan 审查结论」#4。
- settings ch08 块六字段照 Plan 落(`mcp_logistics_url / mcp_aftersales_url / tool_default_timeout=5.0 / mcp_tool_timeout=10.0 / tool_max_retries=2 / demo_ticket_delay_seconds=0.0`);`.env.example` 尾部追加三行。
- `sql/ch08-ddl.sql` 补建(缺口 #1);dev 库已应用,`information_schema.COLUMNS` 核验 status 枚举五个中文值无 double-encode(ch06 教训:第二条 DESC 命令不带 `--default-character-set` 会显示 `??`,纯客户端显示问题)。
- TDD 红绿:`tests/db/test_tool_audit.py` 2 测(ImportError 先红)→ 实现后 2 passed;conftest `_DDL_FILES` 追加 ch08、`_TABLES` 插入 `tool_audit_logs`(tickets 之后、conversations 之前);模型/仓库函数照 Plan 原文。
- 全量回归:**228 passed / 0 failed**(ch07 基线 226 + 本章 2)。
- 无新增待 key 项(零模型)。

## Task 2:ToolSpec 注册中心 + builtin/ 包(注册即定义,query_logistics 下线)

- TDD 红绿:`tests/tools/test_registry.py` 5 测(AttributeError 先红)→ 实现后全过;全量回归 **233 passed / 0 failed**(228 + 5)。
- 结构:`registry.py` 重造(ToolSpec 七字段 + register 重名丢弃告警 + scan_builtin 幂等包扫描 + permission_for 只认 WRITE_TOOLS + spec_from_langchain_tool 用 tool_call_schema 出模型可见口径);`builtin/` 四模块注册即定义(orders/faq/tickets/refunds);business.py 瘦身为纯函数数据源(order_snapshot/list_user_orders,fetch_order 节点签名不动);main.py lifespan 里 `scan_builtin()` 先于 init_graph。
- **兼容段偏差**:过渡 `NO_RETRY` 保持旧两件套 `{"create_ticket","submit_refund"}`,未按 Plan 片段加 `query_faq`——加了会让 `test_tools_infra::test_retry_succeeds_on_second_attempt`(Plan 明令本任务不动)必红;engine(Task 3)按 `permission=="write"` 判不重试,不读 NO_RETRY,无后续影响。
- 连带 grep 扫描清算(Plan Step 5 + 审查结论 #3):`tests/test_tools_mock.py` 物流用例随工具下线删除;`tests/test_tool_ticket.py`、`scripts/eval_ch04.py`(被 test_faith_cases_api 连带收集)改 import 到 `app.tools.builtin.*`;`tests/test_query_faq_rag.py` 的 `business.query_faq` 引用(4 处)改 `builtin.faq`。`scripts/bare_agent_loop.py` 走兼容段暂不动(Task 5 清算);`scripts/eval_agent.py` 是 ch02 旧验收脚本,期望内置物流自调,ch08 后该脚本失效,**待 key 待办挂注记**;`app/core/prompts.py:40` 文案留待 Task 5/6 改 prompt 时一并更新。
- 无新增待 key 项。

## Task 3:统一执行引擎 engine.py(六段管道 + 审计)

- TDD 红绿:`tests/tools/test_engine.py` 10 测(ModuleNotFoundError 先红)→ 实现后 10 passed;全量回归 **243 passed / 0 failed**(233 + 10)。
- 管道落实:①查工具(未知→失败+审计)→ ②Draft 2020-12 校验(best_match 取最相关错误,拦下回灌不抛)→ ③权限门(write 未带 confirmed 拒绝,deny_note 可换文案)→ ④执行(asyncio.wait_for 超时 + _TRANSIENT 重试退避 0.2s×n,写操作恒 retries=0)→ ⑤分诊(超时/网络类如实说「暂时不可用」,业务异常给类型名,禁止模型编结果)→ ⑥格式化(JSON 文本解包 + format_result 钩子 + ensure_ascii=False)+ 审计(result_summary 500 字截断)。
- 测试桩按 Plan Step 3 注记简化:audits fixture 只收 kwargs(引擎全关键字传参,等价成立);注入在**校验后**(schema additionalProperties=False 也不冲突)有专项测试钉死。
- infra.py 本任务未动(旧执行类用例留到 Task 5 随文件一起删);`test_timeout_then_error` 等旧测继续覆盖旧路径,不与新引擎语义冲突(路径独立)。
- 无新增待 key 项。

## Task 4:两台业务 MCP Server(Streamable HTTP)+ 起停脚本

- 实装裁定(mcp 1.30.0):`FastMCP(name, host=…, port=…)` + `run(transport="streamable-http")`,默认挂载 `/mcp`(Settings.streamable_http_path 实测)——与 Plan「旧 FastMCP 兜底」分支一致;`MOCK_DELAY_SECONDS`/`PORT` 环境变量钩子照落。
- **adapters 0.3.2 实装偏差(预授权记录)**:`tool.ainvoke(...)` 返回 **content blocks 列表** `[{'type':'text','text':'<JSON 文本>','id':'lc_<每次调用唯一>'}]`,不是解包 dict——块级 id 使列表级相等恒假,Plan 原断言 `r1 == r2` 不成立。处理:①集成测试稳定性断言改落在 `r1[0]['text']`(业务 JSON)上;②`engine._format_content` 前置 `_unwrap_mcp_content` 解包段(全 text 块 → 拼/取 text,再走原有 JSON→dict→format_result 流程),配套 engine 专项测试(断言块级元数据不进回灌)。中文经 HTTP 全程 utf8mb4 无损(「杭州分拨中心 已发出」原样往返;首测见到的乱码是本机 GBK 终端显示,非数据问题)。
- 集成测试:`tests/tools/test_mcp_servers.py` 子进程真起两台(端口 18101/18102),`get_tools` 三工具名/描述/JSON Schema 断言 + 真调稳定 mock;Plan 的 `time.sleep(2)` 按 Plan 自带授权换成**轮询探活**(20s 内 get_tools 连通,消除启动竞态)。
- Makefile mcp-up/mcp-down 已加(配方 tab `cat -A` 核验);本机无 make,POSIX 起停留待 Task 9 冒烟时以 PowerShell 等价操作验证(dev.sh 的 `make mcp-up` 前置同理)。
- TDD:集成 2 测伴随实现调通(集成测试无传统红绿);全量回归 **246 passed / 0 failed**(243 + 2 集成 + 1 engine 解包)。
- 无新增待 key 项(MCP 栈全本地)。
