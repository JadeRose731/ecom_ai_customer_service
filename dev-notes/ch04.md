# dev-notes · ch04 混合检索 + 重排 + 评估体系

> 按 spec「工作要求 #2」追记:用户关键原话 / 关键产出 / 拒绝与纠偏 / 翻车与返工。

## ⚠️ 红线卡点(冒烟 GO/NO-GO)

- **Milvus BM25 + hybrid_search:GO** ✅(无需 key)。注意本仓库实际镜像是 **v2.6.0**(ch03 迁 Standalone 时装的),不是 plan 写的 v2.5.4——BM25 Function / chinese analyzer / RRFRanker 在 2.5+ 都支持,2.6 实测通过,记录实际 tag=2.6.0,选型内。
- **rerank 链路:待 key(红线暂缓,非 NO-GO)** ⏳。`POST https://api.siliconflow.cn/v1/rerank` 用占位 key 得 **401 Unauthorized**——路径与请求形状是对的(404 才说明形状错),只差真 key。用户说好「密钥最后提供」,故不阻塞本章代码工作;**给 key 后第一件事补跑 `make smoke-rag` 确认 GO**(脚本已就位:`scripts/smoke_rerank.py`)。

## 2026-09-10 开工

- **用户原话**(承接 ch03,继续有效):「不用,按照文档从ch01开始跑,ch01完成后就推送一次,ch02……以此类推,数据库的账号密码都是root,其他密钥先占位,我最后提供。」;追加授权:「如果发现走不通,按照推荐的来,并作记录」。
- **产出**:通读 `ch04/Plan/ch04.md`(20 任务)与 `ch04/Spec/ch04.md`。

## 计划与现状的差异(ch03 已提前做完的部分)

- **Plan Task 1 Step 1(docker-compose 加 Standalone)在 ch03 已完成**:当时因 Windows 无 milvus-lite 包、用户拍板提前迁 Standalone。本次只补 Makefile `milvus-up/milvus-down`,且服务名按本仓库惯例(`milvus/milvus-etcd/milvus-minio`),不是官方文件的 `etcd/minio/milvus-standalone`。
- ch03 已实证两件 ch04 直接受益的事:① BM25 全文检索需要 Standalone——已经在了;② **Bounded 一致性下 upsert 后要 flush 才立即可检索**(Lite 是即时的)——BM25 冒烟里已带上 `client.flush(COLL)`。
- **COSINE distance 语义**:Standalone 返回相似度本身(越大越相似),冒烟 BM25 立查已验证 hybrid 融合正常。ch03 的 `search()` 把 distance 当 score 返回,与 Standalone 语义一致,无返工。

## 待 key 待办(占位密钥下无法验证,用户给 key 后第一时间补跑)

1. `make smoke-rag` —— rerank 半边 401 待真 key;BM25 半边已 GO
2. Task 6 Query 理解标注验证(真实 glm ≥4/5)
3. `make kb-vectorize` → `make eval-rag` 全量(检索段 vector/hybrid 路需嵌入;生成段需聊天上游)
4. 浏览器验收 3/4/5(引用可点 / 拒答落池 / 反馈)——需要真实问答链路
5. Task 20 `make judge-check` 裁判一致率
6. T19 评估脚本→台账接线(判出→写库→复发)端到端:需真实上游产出忠实度 False

## 2026-09-10 T12–T17 阶段(语料 → 评估集 → 评估脚本 → 建库 → 前端 → 评估页)

### 关键产出

- **ch04 语料入库**:`data/kb/` 四文档(新增 `product-specs.md` 12 个易混型号族),切块 **44 块 = spec §7 的 44**,dev 库 `make kb-build` 实测一致(faq 14 / policy 9 / manual 8 / spec 13)。`sources.py` 增 `spec` 类型。
- **评估集 80 题**(`tests/data/eval_ch04.jsonl`):A_policy/B_model/C_colloquial/D_absent 各 20、难度递增。两道机器校验:①结构(80 行/4×20/D 桶规则)②**语料校准**(每条 expect_section 至少命中一个真实 chunk、expect_points 去空白后逐字落在匹配块正文里)。D 桶 20 主题逐个核实库中不存在(货到付款/以旧换新/活体/寄养/疫苗/驱虫药等)。
- **评估脚本**(`scripts/eval_ch04.py` + `make eval-rag`):段1 检索 Recall@10/MRR + 段2 证据覆盖度共用一次检索;段3 生成(45s/调用超时 + 低并发 3 + return_exceptions 单点隔离),忠实度复用 hybrid_rerank 已生成答案不重复调;`generation_done=false` 照样落盘。**评估的就是线上的**:四策略全走 `search_knowledge(strategy=...)`,拒答走线上 `query_faq` 端到端。
- **dev 建库的无 key 变通**:schema 的 dense 字段必填,没嵌入 API 就一行都灌不进去。dev 专用一次性灌入:44 块真实 text + **确定性 hash 占位向量**,**不回填 MySQL status**(pending 保留)——真 key 的 `kb-vectorize` 按 PK=id 原位覆盖,不会留脏数据。占位向量下 vector/hybrid 无意义、**BM25 完全真实**(sparse 由 BM25 函数从 text 生成)。
- **聊天页可点引用 + 反馈**(T15,Vibe):`[n]` 角标事件委托挂 document(气泡逐帧重绘不死),来源浮层显示 section_path+原文,👍/👎 一次性锁定。node 独立验证了 inline 的 `[n]` 替换与 markdown 链接共存。
- **RAG 评估页**(`/rag-eval`,T16):API TDD 5 例(缺失=200+present=false、一字不差透传、best 跟总体 MRR 换、generation=null 照样端检索段、坏 json 按没跑过);页面六块=报告头/四 KPI/检索分组柱状图(三指标切换+读图句)/生成质量(覆盖度+忠实度横条+SVG 圆环)/汇总表(best 行底色)/作业日志窗口,job-done 自动重取。admin 第五张卡、导航加「RAG 评估」、jobs 注册 eval-rag(heavy)。

### bm25-only 真实报告(验收2 的数据侧先证一半)

`EVAL_STRATEGIES=bm25` 对 dev 真集合跑通全管线(R@10/MRR/证据覆盖度/报告落盘/页面出数),数字已见策略分化:

| 桶 | Recall@10 | MRR |
|---|---|---|
| A_policy | 1.000 | 0.863 |
| **B_model(型号题)** | **1.000** | **0.975** |
| **C_colloquial(口语题)** | **0.750** | **0.536** |
| 总体 | 0.917 | 0.791 |

证据覆盖度 0.925。**B 桶 MRR 0.975 vs C 桶 0.536**——BM25 吃型号词、瘸口语问法,正是验收2 要的实证;生成段全超时(占位上游),`generation_done=false` 优雅降级,报告照样落盘、页面照样出数(标「生成段本次未完成」)。CLI 侧:`智能猫砂盆Pro 型号` 纯 BM25 命中 LP50/LP100/LP200 前三。

### 偏差与解释(不叫返工,但记下来)

- **kb-reset 适配 Standalone**:plan Task 14 要求改 Makefile 删 Lite 文件——ch03 迁移时 `scripts/reset_kb.py` 已是 drop 集合版,无需再动,record。
- **评估集校准提前到 T12**:plan 流程是 T12 出集、T14 建库后校准。实际用 `dump_chunks.py` 先打印 44 块真实 section_path/answer 再写题,建库后零回填(同一条 build_chunks 代码路径)。
- **`EVAL_STRATEGIES` env 与 `done` 标记**:占位 key 下 vector/hybrid 全超时,四策略全跑只会得到三列假 0——加 env 键只跑能真实跑的策略;`done` 标记防「全 401 但 generation_done=true」的误导。全量四策略跑仍以 plan 为准(待key)。
- **plan 的 rerank payload 缩进 bug**:Task 5 片段把 payload 写在 `if not docs: return []` 之后(死代码),实现时提到函数级。
- **admin 卡测试口径**:第四张卡变第五张,`test_admin_api` 的全 unreachable 断言改成「库依赖三卡 unreachable + 文件型 rageval 卡自报自态」——rageval 只读文件,不该连坐。

### 全量回归(T17 Step 1)

121 passed(116 + rageval 5),Milvus Standalone 起着,55s。浏览器验收 3/4/5 需真实问答链路,待key(占位上游连 query 理解都过不去,SSE 直接报「上游模型暂时不可用」)。

## 2026-09-10 T18(评估集 300 题五桶 + Recall@5 + 校验脚本)

### 关键产出

- **评估集扩到 300 题**(`tests/data/eval_ch04.jsonl`):原 80 题原样保留,新增 A21-A60 / B21-B60 / C21-C60 / D21-D60 / **E_multi 1-60**。E 桶跨文档题用 `expect_sections_all`(组列表,组内别名任一命中即算该组),每题 2~3 组证据 + 扁平 expect_points,不带 expect_section 键。
- **守门脚本**(`scripts/validate_eval_ch04.py` + `make eval-check`):id/query 唯一、五桶各 60、每组别名至少命中一个真实 section_path、要点逐字(去空白)落在匹配小节正文、D 桶必须空证据+拒答。语料从 `data/kb/*.md` 现场切块(与入库同一条 build_chunks 路径),**不需要 MySQL/Milvus 在场**。首跑抓到 1 处校准错(E57 把「不含摄像头与 App 远程投喂」写成了不连续子串),修正后 300 题/302 证据组/356 要点全过。
- **口径**:`RECALL_K=5`(检索深度 K=10 不变)——300 题量级下把「进前 10」收紧成「进前 5」;A–D 单组证据 Recall 看前 5 名、MRR 用全窗口;E 桶 Recall=命中组数/总组数(凑齐才 1.00)、MRR=各组名次倒数取平均、漏的记 0。meta 增 `recall_k`,页面 Recall 标签跟它走。
- **`scripts/populate_dev_vectors.py` + `make dev-vectors`**:占位灌库从一次性 inline 升级成正式脚本。行构造与 `dualwrite.vectorize_pending` 完全一致(text/category/questions/answer 同源,sparse 由 BM25 Function 真实生成),dense 用按 chunk id 种子的 sha256 占位向量;**只灌 pending 块、绝不碰 done 块**,不回填 MySQL status。
- **`EVAL_SKIP_GENERATION=1`**:跳过生成段的 env 开关(与 EVAL_STRATEGIES 同款理由)——dev 无 key 时生成段只会全超时,300 题下要空烧 ~45 分钟,跳过后秒级拿到检索段报告。

### 翻车与返工

- **跑评估发现四策略全 0**:排查是 Milvus `knowledge` 集合被清空——**pytest 的 milvus fixture 每用例 drop/重建集合,跑完全量回归后集合就是空的**(T17 回归把 T14 灌的库抹了,当时 bm25 报告是在回归之前跑的所以没暴露)。教训固化为:`make dev-vectors` 现在是回归之后跑任何真集合评估的标准前置动作,记入 README。
- E57 校准错(见上),validator 的价值首跑就兑现。

### plan 偏差

- **plan Task 18 Step 4 提到 `app/api/observability.py` 的 recall_at_5/recall_at_10 兼容项——本章不存在该文件**(app/api/ 下只有 admin/agent/chat/extract/jobs/kb/rageval),推断属于后面可观测性章节的接口,届时再对齐,此处仅记录。
- bm25-only 300 题报告(EVAL_SKIP_GENERATION=1):A R@5=0.900/MRR 0.793,B **1.000/0.992**,C 0.700/0.546,**E 0.942/0.621**,总体 0.885/0.738,证据覆盖度 0.925。B≫C 的分化在 5 倍题量下依旧;E 桶 MRR 明显低于 Recall——两块证据常只有一块进前列,正是跨文档题的难点画像。

## 2026-09-10 T19(编造个案台账:faith_cases)

### 关键产出

- **faith_cases 表**(并入 `sql/ch04-ddl.sql`):eval_id 唯一键(一题一行)、citations JSON(Top-K 证据快照:n/问法/正文/section_path/chunk id)、status 三态(unresolved/resolved/wontfix)、resolution(处置说明)、seen_count(跨轮复发)、last_seen_at/resolved_at。dev 库已建表(DDL 全文件重放会撞已有表,用 `sed -n '/编造个案台账/,$p'` 只抽新段执行)。
- **仓储四个函数**:`upsert_faith_case`(存在则更新快照+seen_count+1,**已处置过的再判出来退回未解决并清说明/处置时间,返回 relapsed 标记**)、`list_faith_cases`(**未解决排前**(SQL case 表达式),带不受筛选影响的三状态计数)、`set_faith_case_status`(退回连说明与处置时间一起清)、`faith_case_status_map`(报告侧把本轮判出对回台账状态)。
- **API**(挂 `/api/rag-eval` 下,唯一写入口):GET `/faith-cases`(分页+状态筛选,筛选值非法 422)、POST `/faith-cases/{id}/status`(pydantic Literal 挡脏值 422;**已解决/无需解决说明必填,空白也算没填 → 400**;未知 id 404)。overview 透传新增 `hallucination` 块(仍不在 web 层算指标)。
- **评估脚本接线**:忠实度判出 False 的题 → `upsert_faith_case`,citations 用 `_citations_snapshot`(与 evidence 拼接同序,答案 [n] 即序号);写库失败只打一行日志不拖垮报告。报告新增 `hallucination = hallucination_round(judged, n, status_map)`:**判出率只算本轮(分子=本轮题号数),台账累计单独走 ledger**——纯函数可单测,「台账 3 条、本轮 1 条 → 1/300」有测试钉死。
- **页面台账块**(/rag-eval):状态页签(带计数)/分页/`.fcase` 卡片(左边框色=状态、复发 ×N 徽标、回答全文折叠、证据快照折叠且**答案引用的 [n] 高亮**);已解决/无需解决先展开行内输入、填了才提交,卡片显示「怎么解决的/为什么不用改」;退回未解决一键直发。KPI 区加「编造判出率(只算本轮)」。job-done 后报告+台账一起刷新。

### 测试

`tests/test_faith_cases_api.py` 9 例:一题一行、复发退回、分页+筛选+未解决排前、证据快照 round-trip、处置必填(空白 400)、退回清说明、非法状态 422、未知 id 404、判出率 1/300 口径。

### plan 偏差 / 待key

- plan Step 4 提到个案可带「理由」——当前裁判 `_Faith` 只回 bool,没有理由字段可存,故 DDL/model/upsert 均未设 reason 列;T20 若把裁判改结构化输出再补。
- 评估脚本→台账的接线(判出→写库)依赖真实上游产出忠实度 False,占位 key 下全 None,该路径**待 key 后首次全量 eval-rag 才能端到端验证**(已记入待 key 待办)。
