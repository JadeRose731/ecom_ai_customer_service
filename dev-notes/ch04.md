# dev-notes · ch04 混合检索 + 重排 + 评估体系

> 按 spec「工作要求 #2」追记:用户关键原话 / 关键产出 / 拒绝与纠偏 / 翻车与返工。

## ⚠️ 红线卡点(冒烟 GO/NO-GO)

- **Milvus BM25 + hybrid_search:GO** ✅(无需 key)。注意本仓库实际镜像是 **v2.6.0**(ch03 迁 Standalone 时装的),不是 plan 写的 v2.5.4——BM25 Function / chinese analyzer / RRFRanker 在 2.5+ 都支持,2.6 实测通过,记录实际 tag=2.6.0,选型内。
- **rerank 链路:待 key(红线暂缓,非 NO-GO)** ⏳。`POST https://api.siliconflow.cn/v1/rerank` 用占位 key 得 **401 Unauthorized**——路径与请求形状是对的(404 才说明形状错),只差真 key。用户说好「密钥最后提供」,故不阻塞本章代码工作;**给 key 后第一件事补跑 `make smoke-rag` 确认 GO**(脚本已就位:`scripts/smoke_rerank.py`)。

## 2026-09-10 开工

- **用户原话**(承接 ch03,继续有效):「不用,按照文档从ch01开始跑,ch01完成后就推送一次,ch02……以此类推,数据库的账号密码都是root,其他密钥先占位,我最后提供。」;追加授权:「如果发现走不通,按照推荐的来,并作记录」。
- **产出**:通读 `ch04/Plan/ch04.md`(20 任务)与 `ch04/Spec/ch04.md`。

## 计划与现状的差异(ch03 已提前做完的部分)

- **Plan Task 1 Step 1(docker-compose 加 Standalone)在 ch03 已完成**:当时因 Windows 无 milvus-lite 包、用户拍板提前迁 Standalone。本次只补 Makefile `milvus-up/milvus-down`,且服务名按本仓库惯例(`milvus/milvus-etcd/milvus-minio`),不是官方文件的 `etcd/minio/milvus-standalone`。
- ch03 已实证两件 ch04 直接受益的事:① BM25 全文检索需要 Standalone——已经在了;② **Bounded 一致性下 upsert 后要 flush 才立即可检索**(Lite 是即时的)——BM25 冒烟里已带上 `client.flush(COLL)`。
- **COSINE distance 语义**:Standalone 返回相似度本身(越大越相似),冒烟 BM25 立查已验证 hybrid 融合正常。ch03 的 `search()` 把 distance 当 score 返回,与 Standalone 语义一致,无返工。

## 待 key 待办(占位密钥下无法验证,用户给 key 后第一时间补跑)

1. `make smoke-rag` —— rerank 半边 401 待真 key;BM25 半边已 GO
2. Task 6 Query 理解标注验证(真实 glm ≥4/5)
3. `make kb-vectorize` → `make eval-rag` 全量(检索段 vector/hybrid 路需嵌入;生成段需聊天上游)
4. 浏览器验收 3/4/5(引用可点 / 拒答落池 / 反馈)——需要真实问答链路
5. Task 20 `make judge-check` 裁判一致率
