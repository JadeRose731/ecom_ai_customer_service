# dev-notes · ch02 Function Calling 工具链

> 按 spec「工作要求 #2」追记:用户关键原话 / 关键产出 / 拒绝与纠偏 / 翻车与返工。

## 2026-09-09 开工

- **用户原话**(承接 ch01 收尾):「不用,按照文档从ch01开始跑,ch01完成后就推送一次,ch02……以此类推,数据库的账号密码都是root,其他密钥先占位,我最后提供。」
- **产出**:通读 `ch02/Plan/ch02.md`(14 任务)与 `ch02/Spec/ch02.md`。

## 阶段判断:Task 1 冒烟顺延(不阻塞实施)

- Plan 把「glm-5.2 tool-calling 冒烟」设为 go/no-go 风险闸,NO-GO 时停下问用户。
- 现状:`.env` 的 `CHAT_API_KEY` 为占位符(用户明确说最后提供),冒烟无法执行,也**不能**据此判 NO-GO 换方案。
- **处理**:视为用户已预授权「密钥后补、实现先行」——脚本照写(`scripts/smoke_toolcall.py`),Task 2 起继续;冒烟与 Task 14 eval/浏览器验收列为**待 key 待办**,用户给 key 后第一时间补跑并回填本文件。

## 计划缺口补齐(主动上报)

- Plan 把 `sql/ch02-ddl.sql` 当权威建表文件(docker initdb 与 conftest 都消费),但仓库中不存在。
- **处理**:按 Task 3 的 ORM 映射与 spec 的表结构忠实补写该文件(4 张表、utf8mb4、中文 ENUM、DB 默认值),不引入 Plan 外字段。这是补文件而非改选型。

## 环境适配(承接 ch01)

- Windows 11,无 make → 保留 Makefile,实际 `uv run` / Git Bash 直跑。
- Docker Desktop 需手动拉起(daemon 初始未运行),已就绪后再 `docker compose up -d`。
- **端口冲突**:本机 3306 被 Windows 版 MySQL 服务(PID mysqld)占用,不动用户本机服务,把 Docker MySQL 映射为 **3307:3306**,`config.py` 默认值与 `.env`/`.env.example` 同步改 3307。
- 中文经控制台传参仍是 GBK 坑(curl body、`mysql -e "LIKE '%中文%'"` 均踩过)→ 统一改走 UTF-8 文件(Write 工具写文件再 `--data-binary @file` / `< file` 管入)。

## 阶段 3 · Task 2–13(按 Plan 逐任务 TDD,先红后绿)

- **产出**(commit 后缀省略 Co-Authored-By):
  - Task 2 基建:补写 `sql/ch02-ddl.sql`(Plan 引用为权威但仓库缺失,按 ORM/_spec 表结构忠实补齐,无注释——conftest 按 `;` 切分后仅执行 `CREATE TABLE` 开头语句);docker-compose(MySQL 8,3307);conftest 三 fixture;`test_db_conn` 绿
  - Task 3 ORM 4 表;Task 4 repository 4 用例;Task 5 seed 6 行幂等( `%退货%` 命中/`%鞋子%` 空,验收 2/3 数据设计成立)
  - Task 6–8 工具:3 mock(种子可复现)+ query_faq + create_ticket
  - Task 9 registry+infra(超时/重试/写类不重试/错误回灌)5 用例
  - Task 10 AGENT_SYSTEM 在场断言;Task 11 编排共享核心+双出口 7 用例;Task 12 接口层新契约 7 用例(全量回归 **51 passed**)
  - Task 13 聊天页 `app/static/index.html`(Vibe Coding:工具徽章/markdown 渲染/打字点/会话持久化)+ main.py 挂载静态目录
- **翻车与返工**:
  1. `asyncmy` 连 MySQL 8 默认 `caching_sha2_password` 认证报缺 `cryptography` → `uv add cryptography`,非选型变更。
  2. pytest-asyncio 1.x 默认 function 级 loop,session 级 `_test_engine` 跨 loop 报 "Future attached to a different loop" → pyproject 增加 `asyncio_default_fixture_loop_scope/test_loop_scope = "session"`。
  3. ORM `server_default` 列 INSERT 后未加载,异步属性访问抛 `MissingGreenlet` → 四个 mapper 加 `__mapper_args__ = {"eager_defaults": True}`(映射语义不变)。
  4. Plan Task 8 断言 `get_input_schema()` 不含 `conversation_id`,实测该方法是执行用完整 schema(injected 参数设计上在);真正暴露给模型的是 `tool_call_schema`(实测不含 `conversation_id`,`InjectedToolArg` 生效)→ 按 Plan「API 偏差以官方文档+实测为准」改断言对象,**未走 Fallback**。
  5. Task 11 编写时漏抄 `AIMessageChunk` 导入与 `get_chat_model` 导入,测试即时暴露,补上即绿。

## 完结交付与待 key 待办

- **已交付**:全部代码 + 51 个单测全绿;`/api/chat` 工具链 SSE、`/api/agent` 程序化出口、聊天页;eval/demo 脚本与 README。
- **待真实 key 后补跑**(dev-notes/ch01 同款清单):
  1. `uv run python scripts/smoke_toolcall.py` —— Task 1 go/no-go 冒烟,结论回填本文件
  2. `make eval-agent` —— 7 条标注样例,核对选工具;重点记录「邮费是多少」漏召回实测结果(留 ch03)
  3. 浏览器三条验收(徽章/打字点/markdown/新对话按钮)
  4. `./scripts/demo_agent.sh` 留档
- 说明:聊天页浏览器实测在占位 key 下只能验「页面可达 + 错误帧展示」,真实工具徽章链路待 key。
